# character-id-3.graphql

query GetCharacterById {
    character(id: 3) {
        id
        name
        status
        species
        type
        gender
        origin {
            name
        }
        location {
            name
        }
        image
        episode {
            id
            name
            air_date
        }
    }
}